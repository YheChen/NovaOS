# @novaos/&lt;name&gt;

## Purpose

One paragraph: what domain truth or capability this package owns.

## Public API

The interfaces and functions exported from `src/index.ts`.

## Events

The typed `DomainEvent`s this package emits (if any).

## Snapshots

The serializable, versioned snapshot shapes this package exposes (if any).

## Testing

How this package is tested (unit / golden / replay / integration).

## Dependency Rules

Allowed dependencies, forbidden dependencies, and why.
