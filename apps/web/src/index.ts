/**
 * @novaos/web — NovaOS browser application shell.
 *
 * Scaffold only (Milestone 0). Implementation begins in Milestone 7.
 */
export const PACKAGE_NAME = '@novaos/web';
export const VERSION = '0.0.0';
