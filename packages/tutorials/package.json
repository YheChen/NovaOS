{
  "name": "@novaos/tutorials",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Guided tutorial dataset + pure checkpoint verifiers spanning the NovaOS lab.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@novaos/examples": "workspace:*"
  },
  "devDependencies": {
    "@novaos/simulator": "workspace:*"
  }
}
