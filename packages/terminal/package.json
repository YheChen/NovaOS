{
  "name": "@novaos/terminal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Terminal session model, history, autocomplete, and I/O.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@novaos/events": "workspace:*",
    "@novaos/filesystem": "workspace:*",
    "@novaos/shared": "workspace:*",
    "@novaos/shell": "workspace:*"
  },
  "devDependencies": {
    "@novaos/assembler": "workspace:*",
    "@novaos/cpu": "workspace:*",
    "@novaos/kernel": "workspace:*",
    "@novaos/memory": "workspace:*",
    "@novaos/scheduler": "workspace:*",
    "@novaos/simulator": "workspace:*",
    "@novaos/testing": "workspace:*"
  }
}
