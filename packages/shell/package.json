{
  "name": "@novaos/shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shell lexer, parser, AST, and command execution.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  }
}
