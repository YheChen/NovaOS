{
  "name": "@novaos/mmu",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Educational virtual-memory simulator: page tables, VA decode, demand paging with FIFO/Clock replacement, and an optional TLB. Models its own physical frames; standalone from the CPU/kernel.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@novaos/shared": "workspace:*"
  }
}
