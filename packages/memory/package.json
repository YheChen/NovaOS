{
  "name": "@novaos/memory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Byte-addressable RAM, memory segments, first-fit allocator, stack/heap.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@novaos/shared": "workspace:*"
  }
}
