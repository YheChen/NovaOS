{
  "name": "@novaos/filesystem",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Virtual filesystem, path resolution, file operations, persistence.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  }
}
