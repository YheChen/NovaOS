/**
 * @novaos/kernel — Boot lifecycle, process table, syscalls, interrupts, context switching.
 *
 * Scaffold only (Milestone 0). Implementation begins in Milestone 2.
 */
export const PACKAGE_NAME = '@novaos/kernel';
export const VERSION = '0.0.0';
