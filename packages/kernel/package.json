{
  "name": "@novaos/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Boot lifecycle, process table, syscalls, interrupts, context switching.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@novaos/cpu": "workspace:*",
    "@novaos/events": "workspace:*",
    "@novaos/memory": "workspace:*",
    "@novaos/scheduler": "workspace:*",
    "@novaos/shared": "workspace:*"
  },
  "devDependencies": {
    "@novaos/testing": "workspace:*"
  }
}
