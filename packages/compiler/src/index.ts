/**
 * @novaos/compiler — Toy C pipeline: lexer, parser, semantic analysis, IR, codegen.
 *
 * Scaffold only (Milestone 0). Implementation begins in Milestone 5.
 */
export const PACKAGE_NAME = '@novaos/compiler';
export const VERSION = '0.0.0';
